/**Combinar listas de invitados

Tema: concat()

const invitadosA = ['Carlos', 'Lucía']
const invitadosB = ['Pedro', 'Sofía']
Combina ambos arreglos en uno nuevo y muéstralo. */

const invitadosA = ['Carlos', 'Lucía'];
const invitadosB = ['Pedro', 'Sofía'];
const invitadosTotales = invitadosA.concat(invitadosB);
console.log(invitadosTotales);